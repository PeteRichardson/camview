# /code-review reports are ephemeral snapshots — regenerated by --refresh,
# with ordinals that reset each run. Deliberately NOT a blanket reviews/ rule:
# PROJECT_REVIEW.md lives in the same directory and is meant to be committed,
# so /project-review can recover finding IDs (F1, F2, ...) from git history.
reviews/code-review*.md
